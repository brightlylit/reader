import React from 'react';
const PGddlFeedback =  React.forwardRef(( props, ref ) => {
    return (
            <div ref={ props.ref }>
               Feedback 
            </div>
        )
    });
    
   
export default PGddlFeedback;